import 'package:flutter/material.dart';

class SummaryOfSelection extends StatelessWidget {
  const SummaryOfSelection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}